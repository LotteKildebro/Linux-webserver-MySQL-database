<% extend('../partials/template') %>
    <h2>
        <%= title %>
    </h2>

    <% (data || []).forEach(element =>{ %>
        <p>
            <a href="/fisk/<%=element.fisk_id%>">
                <%= element.fisk_navn %>
            </a>
        </p>
        <% }) %>